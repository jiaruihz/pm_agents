interface:
  display_name: "Weather Exposure"
  short_description: "Inspect positions, open orders, valuation, and risk"
  default_prompt: "Use $weather-strategy-exposure to discover current runtimes through the controller and report canonical positions separately from authenticated open orders."
