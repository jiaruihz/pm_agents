interface:
  display_name: "Weather Performance"
  short_description: "Evaluate fee-adjusted performance and forward evidence"
  default_prompt: "Use $weather-strategy-performance to verify controller and canonical DB identity, then evaluate a fixed-denominator strategy with fees, baselines, and forward evidence."
