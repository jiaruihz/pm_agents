interface:
  display_name: "Weather Fact Refresh"
  short_description: "同步或重建天气 canonical facts 并验证完整性"
  default_prompt: "Use $weather-fact-rebuild to verify controller and canonical DB identity, then run only the minimal registered refresh required."
