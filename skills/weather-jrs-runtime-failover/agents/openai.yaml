interface:
  display_name: "Weather JRS Runtime Failover"
  short_description: "诊断 JRS 权限故障并通过 controller 恢复"
  default_prompt: "Use $weather-jrs-runtime-failover to preserve production state and recover JRS only through the registered production controller."
