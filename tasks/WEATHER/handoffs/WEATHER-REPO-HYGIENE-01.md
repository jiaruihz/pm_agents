# WEATHER-REPO-HYGIENE-01 Handoff

STATUS: READY_FOR_REVIEW
IMPLEMENTATION_COMMIT: cc3db8a001d1f45de4bdac0d51e92edb1f9e6ebf
CLEANUP_SET: /Users/deepsleep/.Trash/pm-agents-repo-hygiene-20260828.uOlVkv

## Claims and evidence

1. **Temporary worktrees were removed without losing code.**
   Four clean `/private/tmp` worktrees (`411,896 KiB`) had no process, open
   handle or LaunchAgent consumer.  `git worktree remove` removed only their
   checked-out directories; branches and commits `4db76556`, `f818c0fd`,
   `09fce3c1` and `9e6d821f` still resolve.
2. **Only proven rebuildable repository intermediates were moved.**
   The 60 initially inventoried Python/pytest cache directories (`9,768 KiB`),
   eight caches regenerated by validation, two `.DS_Store` files (`24 KiB`)
   and two empty untracked directories moved to the recoverable cleanup set
   with original relative paths.  The `19,668 KiB` Trash set was not emptied.
   Eleven control-plane Python cache directories (`732 KiB`) regenerated by
   active registered entrypoints and remain intentionally; 49 original cache
   directories did not return.
3. **The recurring log producer is bounded at its source.**
   The canonical refresh log peaked at `83,450,763` bytes.  Commit `cc3db8a0`
   adds opt-in one-shot log compaction; canonical refresh uses a `64 MiB`
   threshold and retains `8 MiB`.  Compaction occurs only after the named tmux
   session is atomically acquired, preventing concurrent inode replacement.
   Unrelated one-shot callers do not opt in and retain prior behavior.
4. **Production behavior and data identity are unchanged.**
   The reinstalled registered refresh completed with exit `0`, new log size
   `16,763` bytes and fill gate PASS.  Strict pre/post manifest comparison is
   `healthy`, with all 32 baseline sessions present.  Controller, JRS context,
   storage identity and data-feed semantics are healthy.  DB/raw fills are
   `1,567/1,567`; fill-ID and DB/fact cost deltas are zero.  Strategy config,
   releases, orders, fills and modes were not changed.
5. **User and research state was preserved.**
   All 34 pre-existing modified paths remain.  Four of the 168 pre-existing
   untracked GLM triage paths became tracked in concurrent commit `7bf760ce`;
   the other 164 remain untracked.  All 202 original paths still exist.
   Large evidence/runtime categories intentionally retained include
   `proposal_reward` (`119,532 KiB`), `dispute_repricing` (`85,384 KiB`),
   `analysis_snapshots` (`50,888 KiB`), `_backups_n100` (`14,040 KiB`),
   `_external_research` (`9,676 KiB`) and harness runs (`2,360 KiB`).

Path-accounted size fell by `134,084 KiB` inside the repository and by another
`411,896 KiB` in removed temporary worktrees.  The recoverable Trash set keeps
`19,668 KiB`; APFS allocation/reflink accounting may differ from these `du`
figures.

## Validation

- `bash -n` on both touched shell files: PASS
- `git diff --check`: PASS
- focused pytest set: `103 passed`
- repository-wide docs checker: FAIL on 21 pre-existing/concurrent findings;
  no task-owned file is named (details retained in the task completion record)
- independent reviewer: one P1 race found and fixed; no second reviewer chain
- canonical LaunchAgent: `last exit code = 0`
- fill coverage gate: PASS
- account reconcile smoke (`2026-08-27..28`): raw/DB missing IDs `0/0`, gate PASS
- post-change strict manifest: healthy, findings `0`, missing sessions `0`
- storage audit: healthy, critical `0`, warning `0`

## Recovery and rollback

- Cache and old-log files can be restored from `CLEANUP_SET`; the Trash was not
  emptied.
- Any removed temporary worktree can be recreated from its preserved branch or
  commit with `git worktree add`.
- Code rollback is `git revert cc3db8a0`; doing so restores unbounded canonical
  refresh logging and is therefore not recommended.

## Reviewer record

Prompt scope was limited to the two shell entrypoints and their focused test.
The reviewer checked correctness, concurrency, quoting, macOS compatibility,
JRS permission-host boundaries and opt-in isolation.  It reported the P1 race
described above; coordinator moved compaction inside the unique session,
added existing-session/non-opt-in tests, and reran the full focused set.  The
reviewer's runtime did not expose token/usage telemetry.
